import { NumberOfRooomsValidator } from './number-of-roooms-validator';

describe('NumberOfRooomsValidator', () => {
  it('should create an instance', () => {
    expect(new NumberOfRooomsValidator()).toBeTruthy();
  });
});

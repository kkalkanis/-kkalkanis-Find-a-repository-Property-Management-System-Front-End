import { NoSamePasswordsValidator } from './no-same-passwords-validator';

describe('NoSamePasswordsValidator', () => {
  it('should create an instance', () => {
    expect(new NoSamePasswordsValidator()).toBeTruthy();
  });
});
